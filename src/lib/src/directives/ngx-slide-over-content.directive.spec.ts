/* tslint:disable:no-unused-variable */

import { async, TestBed } from '@angular/core/testing'
import { NgxSlideOverContentDirective } from './ngx-slide-over-content.directive'

describe('Directive: NgxSlideOverContent', () => {
  it('should create an instance', () => {
    const directive = new NgxSlideOverContentDirective()
    expect(directive).toBeTruthy()
  })
})
